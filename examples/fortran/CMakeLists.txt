IRLAddExample_fortran(
  TARGET_NAME
    f_cutting_methods
  SRCS
    "cutting_methods.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

IRLAddExample_fortran(
  TARGET_NAME
    f_localized_separator_link
  SRCS
    "localized_separator_link.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

IRLAddExample_fortran(
  TARGET_NAME
    f_reconstruction_elvira
  SRCS
    "reconstruction_elvira.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

IRLAddExample_fortran(
  TARGET_NAME
    f_reconstruction_mof
  SRCS
    "reconstruction_mof.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

IRLAddExample_fortran(
  TARGET_NAME
    f_cube_cutting
  SRCS
    "cube_cutting.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

IRLAddExample_fortran(
  TARGET_NAME
    f_polygon_cutting
  SRCS
    "polygon_cutting.f90"
  OUTPUT_DIR
    "${IRL_EXAMPLES_OUTPUT_DIR}/fortran"
  COLLECTION
    "examples_fortran"
)

